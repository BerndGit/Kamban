<Window x:Class="Kamban.Views.CardEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Kamban.Views"

        mc:Ignorable="d"
        Title="Edit Card" SizeToContent="WidthAndHeight" MinWidth="500" MinHeight="400" Background="BlueViolet">
    <Window.Resources>

    </Window.Resources>
    
    
    <Grid  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <views:CardEditView x:Name="cardEditView" Grid.Column="1" Grid.Row="1"
                                  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            
        </views:CardEditView>  
    </Grid>
</Window>

